# HANDOFF v1.1.0

## 1. 基线信息
- 项目路径: `D:\wdapp1\codexwj\codex1`
- 版本标签: `v1.1.0`
- 基线提交: `37981be2e2e115285fd5dd6b19e9c3db2406050d`
- 当前分支: `master`
- 远程仓库: `git@github.com:kangy1360-crypto/yang-tools.git`
- 主代码文件: `csv_wave_viewer.py`

## 2. 代码级核验结论（可承接）
- 已核验 `v1.1.0` 标签与当前 `HEAD` 一致。
- `python -m py_compile csv_wave_viewer.py` 通过。
- 工作区存在未跟踪产物: `*.spec`、`build/`、`dist/`，不影响主逻辑。

## 3. 交互约定核验（11项）
说明: 下列为基于 `v1.1.0` 代码路径的核验结果。GUI 实机点击类冒烟需在桌面环境按检查单再跑一轮。

1. CSV 加载（编码尝试、进度、可取消）: 通过  
2. 自动识别时间列并构建时间轴: 通过  
3. 左侧信号搜索 + 勾选即绘制: 通过  
4. 多子图共享 X 轴联动: 通过  
5. 底部 `region` 控制主视图窗口: 通过  
6. 鼠标悬停右侧显示当前多信号值: 通过  
7. 左键单击子图记录标记点（3槽轮转）: 通过  
8. 标记删除入口在右侧标记框 `×`: 通过  
9. 红色竖线加粗增强可见性: 通过  
10. 子图左键拖动框选时间段并同步全部波形: 通过  
11. 滚轮缩放时间窗口（主图与总览区域均可触发）: 通过

## 4. 关键实现锚点（维护必看）
- 场景级交互统一入口: `eventFilter`（`QGraphicsScene` 事件分发）。
- 点击与拖拽冲突隔离: `_suppress_next_click`。
- 窗口边界裁剪: `_clamp_x_window`。
- 窗口状态源: 底部 `region`（主图由其驱动）。
- 鼠标锚点缩放: `GraphicsSceneWheel` 分支，按指针位置保持缩放跟手。

## 5. 已知风险与注意事项
- 当前回归为“代码路径核验 + 编译核验”，未在本线程执行自动化 GUI 点击。
- 若后续改 `eventFilter` 的鼠标事件顺序，需重点复测“拖拽后误触发单击”问题。
- 标记点与窗口联动依赖 `_refresh_marker_graphics`，改动 X 轴逻辑后需联测标记显示。

## 6. 接手执行规范（建议）
1. 先确认基线三元组: `branch + commit + tag`。
2. 每完成一项需求就提交并打标签。
3. 每次改动后执行: `py_compile + GUI 冒烟`。
4. 禁止提交产物: `build/`、`dist/`、`*.spec`。

